<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!--
        IMPORTANT: Copy the intent-filter block below into your app's AndroidManifest.xml,
        replacing the host with the redirect URI domain provided by BotFather.
        See README for full setup instructions.
    -->

    <!--
    <activity android:name=".MainActivity" android:launchMode="singleTask">
        <intent-filter android:autoVerify="true">
            <action android:name="android.intent.action.VIEW" />
            <category android:name="android.intent.category.DEFAULT" />
            <category android:name="android.intent.category.BROWSABLE" />
            <data android:scheme="https"
                  android:host="app{YOUR_APP_ID}-login.tg.dev"
                  android:pathPrefix="/tglogin" />
        </intent-filter>
    </activity>
    -->

    <queries>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="tg" />
        </intent>
    </queries>

</manifest>
